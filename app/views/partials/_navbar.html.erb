<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">LaunchPad</a>
  <!-- Dropdown on smaller devices does not work, have been unable to find the right fix -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="/providers">Launch Providers</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/about">About Us</a>
      </li>
      <!-- Sign in/out button visibility logic -->
      <% if organisation_signed_in? %>
        <li class="nav-item">
            <a class="nav-link" href="/account"><%= current_organisation.name %></a>
        </li>
        <li class="nav-item">
            <%= link_to "Sign out", destroy_organisation_session_path, method: :delete, class: "nav-link" %>
        </li>
            <% else %>
        <li class="nav-item">
            <%= link_to "Sign in", organisation_session_path, method: :create, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>